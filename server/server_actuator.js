ServerActuator = function() {
  
}

ServerActuator.prototype.actuate = function (grid, metadata) {
  
};

// Continues the game (both restart and keep playing)
ServerActuator.prototype.continueGame = function () {
  
};

ServerActuator.prototype.clearContainer = function (container) {
  
};

ServerActuator.prototype.addTile = function (tile) {
  
};

ServerActuator.prototype.applyClasses = function (element, classes) {
  
};

ServerActuator.prototype.normalizePosition = function (position) {
  
};

ServerActuator.prototype.positionClass = function (position) {
  
};

ServerActuator.prototype.updateScore = function (score) {
  
};

ServerActuator.prototype.updateBestScore = function (bestScore) {
  
};

ServerActuator.prototype.message = function (won) {
  
};

ServerActuator.prototype.clearMessage = function () {
  
};
